__pycache__/
*.pyc
*.pyo
*.tar.gz
*.zip
*.exe
dist/
build/
*.spec
.camoufox-cli/
secret.key
config.json
*.log
.env
.venv/
venv/

# Config files with real credentials
nexagent_config.ini
pc-commander/nexagent_config.ini
SshRemote_V2/sshremote_config.ini
pc-commander/core/sshremote_config.ini
**/sshremote_config.ini
!**/sshremote_config.ini.template
pre_login/pre_login_config.json

# Encrypted config & keys
known_hosts.json
security_state.json

# Logs
logs/
commands.log

# Pip accidental log files (e.g. "=20.7" from malformed pip install args)
=[0-9]*

# Temp
*.tmp
bore_out.txt
bore_err.txt
bore_port.txt

# WoL agent config (contains MAC, IP, bot token)
wol_config.json
**/wol_config.json

# SSH public key (identity fingerprint)
sshremote_key.pub
**/sshremote_key.pub

# Pre-login config (contains bot token)
**/pre_login_config.json

# Binary / instruction assets (never commit)
attached_assets/
